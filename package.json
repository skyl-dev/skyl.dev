{
  "name": "skyl-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b packages/core packages/cli",
    "test": "node --test --experimental-strip-types packages/*/test/*.test.ts",
    "typecheck": "tsc -b packages/core packages/cli --force"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

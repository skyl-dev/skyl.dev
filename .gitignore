*.tgz
*.tsbuildinfo
.DS_Store
.env
.env.local
.next/
coverage/
dist/
node_modules/
out/
packages/cli/bundle.json
# Astro's generated cache. dev.log and dev.json are per-machine and churn on every run, so
# the tree was never clean; types.d.ts and content.d.ts are regenerated by build and check.
apps/site/.astro/
# where the Cloudflare Pages build command clones the registry. The site renders a sibling
# checkout, and a Pages build only ever has this repository, so it fetches one and points
# SKYL_REGISTRY at it.
.registry/

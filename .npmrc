# pnpm refuses to pack a bundled dependency through a symlink, and @skyl/core has to
# travel inside the published tarball: it is a workspace package, not something anyone
# can resolve from the registry. Hoisted linking is what makes `pnpm pack` produce a
# self-contained artifact, and with two dev dependencies it costs nothing.
node-linker=hoisted

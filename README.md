# skyl.dev

The website and CLI for [Skyl](https://github.com/skyl-dev/skyl) — a composable registry of AI
agent skills.

A workspace holding two things that share a frontmatter schema and a section splitter:

- the static site at `skyl.dev`
- the `skyl` CLI, published to npm

## Status

Pre-alpha. Not yet started. The skills format is being validated first, in the `skyl` repo.

## License

MIT. See [LICENSE](./LICENSE).
